<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>日志 on Mozaitii</title><link>https://mozaitii.github.io/cn/</link><description>Recent content in 日志 on Mozaitii</description><generator>Hugo -- gohugo.io</generator><language>en-US</language><managingEditor>Mozaitii@qq.com (Mozaitii)</managingEditor><webMaster>Mozaitii@qq.com (Mozaitii)</webMaster><lastBuildDate>Tue, 31 Jan 2017 00:00:00 +0000</lastBuildDate><atom:link href="https://mozaitii.github.io/cn/" rel="self" type="application/rss+xml"/><item><title>博客图片压缩</title><link>https://mozaitii.github.io/cn/2026/08/02/image-compression/</link><pubDate>Sun, 02 Aug 2026 00:00:00 +0000</pubDate><author>Mozaitii</author><guid>https://mozaitii.github.io/cn/2026/08/02/image-compression/</guid><description><![CDATA[
        <p>写动态配图时放了 10 张手机原图，加起来近 24MB。虽然图片设了懒加载，但一滚动就会全量加载，页面明显变慢。这篇记录一下给博客搭的图片压缩方案——现在已经是<strong>全自动</strong>的了。</p>
<h2 id="先澄清一个误区">先澄清一个误区</h2>
<p>一开始我以为 Hugo 编译会自动压缩图片，查了之后发现不会：<code>static/</code> 目录下的文件是<strong>原样拷贝</strong>到发布目录的，一个字节都不动。Hugo 确实有图片处理能力（<code>.Resize</code>、<code>.Fit</code> 等），但只对走资源管线的图片生效——需要图片是 <strong>page bundle 资源</strong>，并且模板里显式调用处理函数。所以要么手动压缩，要么把图片变成「资源」交给 Hugo 处理。我两个都要了。</p>
<h2 id="现在的方案-两层自动压缩">现在的方案：两层自动压缩</h2>
<h3 id="第一层-提交前-git-钩子自动压缩">第一层：提交前，git 钩子自动压缩</h3>
<p>写了压缩脚本 <code>tools/compress_images.py</code>（基于 Pillow），再配一个 git pre-commit 钩子（<code>.githooks/pre-commit</code>）。每次 <code>git commit</code> 时自动触发：</p>
<ul>
<li>找出暂存区里 <code>content/</code> 下的图片，最长边 &gt;1600px 的自动压到 1600px</li>
<li>转渐进式 JPEG、质量 82，顺带去掉 EXIF（更小也更利于隐私）</li>
<li>压缩后反而变大则保留原图</li>
<li><strong>原图先备份到 <code>raw/_originals/</code></strong>（已 <code>.gitignore</code>，不入库，只备份一次）</li>
<li>幂等：已达标的图不动；且<strong>永不阻塞提交</strong>（Pillow 缺失等只提示放行）</li>
</ul>
<p>这一步管的是<strong>仓库体积和灯箱加载量</strong>——入库的图统一不超过 1600px。</p>
<h3 id="第二层-构建时-hugo-生成-webp-缩略图">第二层：构建时，Hugo 生成 WebP 缩略图</h3>
<p>动态（moments）改成 <strong>page bundle</strong> 模式：每篇动态是一个文件夹，照片放同目录，frontmatter 用 <code>pictures</code> 列文件名。模板里对每张图显式调 <code>.Resize</code>：</p>
<pre><code class="language-go">{{ $thumb := $res.Resize &quot;480x webp q80&quot; }}   {{/* 网格缩略图 */}}
</code></pre>
<p>于是网格里显示的是 <strong>480px 的 WebP</strong>（每张 12–40KB），而不是一两百 KB 的原图；点开灯箱才加载 ≤1600px 的原图。这一步管的是<strong>页面加载量</strong>。</p>
<p>模板是双模式的：能解析成 bundle 资源的走 WebP 缩略图，旧文章里 <code>/images/...</code> 绝对路径的原样渲染，兼容不变。</p>
<h2 id="日常流程">日常流程</h2>
<pre><code class="language-bash"># 1. 照片丢进动态的 bundle 文件夹
#    content/moments/2026-08-02-xxx/
#      index.md
#      pic1.jpg
#      pic2.jpg

# 2. frontmatter 里列出文件名（顺序即显示顺序）
#    pictures: [&quot;pic1.jpg&quot;, &quot;pic2.jpg&quot;]

# 3. 提交，钩子自动压缩
git add .
git commit        # ← .githooks/pre-commit 自动压到 1600px
</code></pre>
<h2 id="新克隆仓库需要做的配置">新克隆仓库需要做的配置</h2>
<p>钩子是通过 git 的 <code>core.hooksPath</code> 指向仓库内的 <code>.githooks/</code> 目录来启用的。但 <code>core.hooksPath</code> 属于<strong>本地仓库配置，不会随克隆带过来</strong>——所以每次新克隆或换机器，都要跑这两条命令：</p>
<pre><code class="language-bash"># 1. 启用钩子目录（指向仓库里已提交的 .githooks/pre-commit）
git config core.hooksPath .githooks

# 2. 安装 Pillow（压缩脚本的依赖）
pip3 install Pillow
</code></pre>
<p>验证是否生效：</p>
<pre><code class="language-bash">git config core.hooksPath   # 应输出 .githooks
</code></pre>
<p>之后正常 <code>git commit</code> 即可，钩子会自动压缩暂存区里的大图。如果没做第 1 步，提交也不会报错——只是图片不会被自动压缩（大图会原样进仓库）。</p>
<h2 id="实际效果">实际效果</h2>
<p>把周末那 10 张手机原图过了一遍新流程：</p>
<table>
	<thead>
			<tr>
					<th>环节</th>
					<th>压缩前</th>
					<th>压缩后</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>入库（钩子压到 1600px）</td>
					<td><strong>24.0MB</strong></td>
					<td><strong>1.9MB</strong>（省 92%）</td>
			</tr>
			<tr>
					<td>网格缩略图（Hugo 480px WebP）</td>
					<td>~200KB/张</td>
					<td><strong>12–40KB/张</strong></td>
			</tr>
			<tr>
					<td>灯箱原图（点开才加载）</td>
					<td>—</td>
					<td>~100–440KB</td>
			</tr>
	</tbody>
</table>
<p>网页上 1600px 宽、一两百 KB 的图已经很清晰，手机上看绰绰有余；网格里滚动加载的是十几 KB 的 WebP，快了一个数量级。原图全部在 <code>raw/_originals/</code> 里留着，随时可取。</p>
<h2 id="一点感受">一点感受</h2>
<p>第一次写这篇时靠的是「意志力」——记得每次手动跑一遍压缩脚本。这次升级成两层自动挡：<strong>提交时钩子压缩入库，构建时 Hugo 压缩略图</strong>。发图只做一件事——把照片丢进文件夹、写个文件名，剩下的交给工具链。</p>
<p>省下来的流量和加载时间，会让博客一直保持轻盈；而省下来的「记得要压缩」的注意力，是更好的收获。</p>

        
        ]]></description></item><item><title>git 常用命令速记</title><link>https://mozaitii.github.io/cn/2026/07/29/git-commands/</link><pubDate>Wed, 29 Jul 2026 00:00:00 +0000</pubDate><author>Mozaitii</author><guid>https://mozaitii.github.io/cn/2026/07/29/git-commands/</guid><description><![CDATA[
        <p>Git 命令行一段时间不使用容易遗忘，常查常新，把常用的记录在此。</p>
<h2 id="克隆仓库">克隆仓库</h2>
<pre><code class="language-bash"># HTTPS 方式
git clone https://github.com/用户名/仓库名.git

# SSH 方式（需配置 SSH Key）
git clone git@github.com:用户名/仓库名.git

# 克隆到指定目录
git clone https://github.com/用户名/仓库名.git 目标文件夹

# 浅克隆，只拉取最新版本（省流量省磁盘）
git clone --depth 1 https://github.com/用户名/仓库名.git

# 含子模块的仓库（如本博客的 theme）
git clone --recursive git@github.com:用户名/仓库名.git

# 克隆指定分支
git clone -b 分支名 git@github.com:用户名/仓库名.git
</code></pre>
<h2 id="拉取远程更新">拉取远程更新</h2>
<pre><code class="language-bash"># 查看远程有哪些新提交（安全，不会自动合并）
git fetch origin

# 拉取远程代码并自动合并到当前分支（一步到位）
git pull

# 更完整的写法，先 fetch 看变更，再手动合并
git fetch origin          # 查看远程更新
git log origin/main       # 看远程分支的提交记录
git merge origin/main     # 确认后合并到本地
</code></pre>
<p>今天就用到了这套流程：<code>git fetch</code> 查看 → <code>git log</code> 确认 → <code>git merge</code> 合并，避免了 <code>git pull</code> 可能带来的意外冲突。</p>
<h2 id="查看提交历史">查看提交历史</h2>
<pre><code class="language-bash"># 简洁版，一行一个提交
git log --oneline

# 只看最近 N 条
git log --oneline -5

# 查看某个远程分支的提交历史
git log origin/main --oneline -10

# 详细日志含分支图
git log --oneline --graph --all
</code></pre>
<h2 id="日常操作">日常操作</h2>
<pre><code class="language-bash"># 查看工作区状态
git status

# 添加文件到暂存区
git add 文件名
git add .              # 添加所有变更

# 提交
git commit -m &quot;提交说明&quot;

# 推送远程
git push origin main

# 一行命令推送新分支到远程并建立追踪
git push -u origin 分支名
</code></pre>
<h2 id="分支操作">分支操作</h2>
<pre><code class="language-bash"># 查看本地分支
git branch

# 查看所有分支（含远程）
git branch -a

# 创建并切换到新分支
git checkout -b 新分支名

# 切换分支
git checkout 分支名

# 删除本地分支
git branch -d 分支名

# 删除远程分支
git push origin --delete 分支名
</code></pre>
<h2 id="撤销与回退">撤销与回退</h2>
<pre><code class="language-bash"># 撤销工作区的修改（未 add）
git checkout -- 文件名

# 撤销暂存区（已 add 但未 commit）
git reset HEAD 文件名

# 回退到上一个提交（保留工作区修改）
git reset --soft HEAD~1

# 回退并丢弃工作区修改（谨慎使用）
git reset --hard HEAD~1
</code></pre>
<h2 id="其他实用命令">其他实用命令</h2>
<pre><code class="language-bash"># 暂存当前工作，切换到其他分支干活
git stash
git stash pop          # 恢复暂存

# 查看文件具体变更内容
git diff
git diff --staged      # 查看已暂存的变更

# 绑定远程仓库
git remote add origin git@github.com:用户名/仓库名.git

# 查看远程仓库地址
git remote -v
</code></pre>

        
        ]]></description></item><item><title>博客的 GitHub Pages 部署</title><link>https://mozaitii.github.io/cn/2026/07/28/github-pages-deploy/</link><pubDate>Tue, 28 Jul 2026 00:00:00 +0000</pubDate><author>Mozaitii</author><guid>https://mozaitii.github.io/cn/2026/07/28/github-pages-deploy/</guid><description><![CDATA[
        <p>本博客部署在 GitHub Pages 上，域名 <code>mozaitii.github.io</code>。从写完一篇草稿到它在网上可访问，中间只隔了一次 <code>git push</code>——剩下全交给 GitHub Actions 流水线，这篇把整套部署方案拆开讲讲。</p>
<h2 id="整体架构-两个仓库">整体架构：两个仓库</h2>
<p>关键在于它其实是两个 GitHub 仓库，分工完全不同：</p>
<table>
	<thead>
			<tr>
					<th>仓库</th>
					<th>内容</th>
					<th>作用</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><code>moz_blog_source</code></td>
					<td>Hugo 源码：<code>content/</code>、<code>layouts/</code>、<code>static/</code>、主题子模块</td>
					<td>唯一手动维护的地方</td>
			</tr>
			<tr>
					<td><code>mozaitii.github.io</code></td>
					<td>编译好的静态站（HTML/CSS/图片）</td>
					<td>GitHub Pages 直接托管它</td>
			</tr>
	</tbody>
</table>
<p>链路是这样的：</p>
<pre><code>写博客 → 源码仓库 moz_blog_source
              │  git push main
              ▼
       GitHub Actions 流水线（编译 Hugo）
              │  推送构建产物
              ▼
       发布仓库 mozaitii.github.io（main 分支）
              │  GitHub Pages 托管
              ▼
          https://mozaitii.github.io/
</code></pre>
<p>注意一个细节：<code>docs/</code>（hugo 的构建输出目录）在源码仓库里是<strong>被 <code>.gitignore</code> 忽略的</strong>，<code>publishDir</code> 也指向它，也就是说源码仓库里只有「配方」，没有「成品」——成品由 CI 每次从源码现编出来。</p>
<h2 id="为什么不用-单仓库-docs-目录-发布">为什么不用「单仓库 + docs/ 目录」发布</h2>
<p>GitHub Pages 有一个更常见的玩法：单个仓库里把 <code>hugo</code> 产物放到 <code>docs/</code> 目录，然后在仓库的 Settings → Pages 里选「Deploy from a branch → <code>docs/</code>」，这样只有一个仓库、不需要额外仓库和 token。</p>
<p>刚开始也这么考虑过，最后还是选了独立发布仓库，原因是：</p>
<ul>
<li><strong>源码和产物不混在同一个历史里</strong>。单仓库意味着每次提交都带着编译产物，历史里全是 <code>docs/</code> 的噪音提交；产物和源码解耦后，<code>main</code> 分支就是干净的内容变更。</li>
<li><strong>CI 每次全量重编</strong>，产物可复现。本地 <code>hugo</code> 产生的中间文件（比如 Hugo 图片处理缓存的 <code>resources/</code>）不会误入库。</li>
<li>发布仓库本身也是一个通用的「静态托管位」,将来如果想挂别的静态站、甚至多语言分站，可以独立控制。</li>
</ul>
<p>代价是要维护一个 PAT（见下文），以及在 <code>.github/workflows/</code> 里多写一个文件。对「免费 + 自动」来说这笔账很划算。</p>
<h2 id="workflows工作流">Workflows工作流</h2>
<p>部署的全部魔法都在 <code>.github/workflows/deploy.yml</code> 这一个文件里：</p>
<pre><code class="language-yaml">name: Deploy Hugo Site to GitHub Pages

on:
  push:
    branches: [main]   # 推到 main 就触发
  workflow_dispatch:   # 也支持在 Actions 页面手动触发

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true        # ← 关键：拉取主题子模块

      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v2
        with:
          hugo-version: 'latest'

      - name: Build
        run: hugo --minify        # 构建产物输出到 docs/（publishDir）

      - name: Deploy to mozaitii.github.io
        uses: peaceiris/actions-gh-pages@v3
        with:
          personal_token: ${{ secrets.GH_PAT }}      # 见下节
          external_repository: mozaitii/mozaitii.github.io
          publish_dir: ./docs      # 把刚编译的 docs/ 内容发布出去
          publish_branch: main     # 推到发布仓库的 main 分支
</code></pre>
<p>四步很简单，但每一行都有讲究：</p>
<ul>
<li><code>submodules: true</code>：主题 <code>hugo-ivy</code> 是 git submodule。默认 checkout 不带子模块，不写这行构建会直接报「找不到主题」。</li>
<li><code>peaceiris/actions-hugo@v2</code>：一个把 Hugo 装进 runner 的 action。只装二进制，很快。<code>hugo-version: 'latest'</code> 意味着站点一直跟随最新版编译。</li>
<li><code>hugo --minify</code>：压缩产物（去空白、精简 HTML）。<code>publishDir</code> 在 <code>config.yaml</code> 里是 <code>docs</code>，所以产物自然落在 <code>./docs</code>。</li>
<li><code>peaceiris/actions-gh-pages@v3</code>：把 <code>publish_dir</code> 指向的文件夹内容推送到另一个仓库的分支。<code>publish_branch: main</code> <code>会整分支覆盖式更新</code>——发布仓库的 <code>main</code> 永远等于「最新一次构建产物」。</li>
</ul>
<h2 id="跨仓库推送的秘密">跨仓库推送的秘密</h2>
<p>这里有个不能绕过的点：workflow 默认拿到的 <code>GITHUB_TOKEN</code> <code>只在当前仓库内部有效</code>，没法往 <code>mozaitii.github.io</code> 这个「别的仓库」里推。所以要显式给一个 PAT（Personal Access Token）。</p>
<p>配置流程：</p>
<ol>
<li>在 GitHub 右上角头像 → <strong>Settings → Developer settings → Personal access tokens → Tokens (classic)</strong>，点 Generate new token。</li>
<li>scope 勾选 <strong><code>repo</code></strong>（包含读写该账号下所有仓库的权限）。更稳妥的做法是用 <strong>Fine-grained token</strong>：只授权发布仓库 <code>mozaitii.github.io</code>，权限只给 <code>Contents: Read and write</code>——这样就算 token 泄露也推不了别的仓库。</li>
<li>回到源码仓库 → <strong>Settings → Secrets and variables → Actions → New repository secret</strong>，名字填 <code>GH_PAT</code>（和 workflow 里 <code>secrets.GH_PAT</code> 对应），值粘贴 token。</li>
</ol>
<p>之后 workflow 运行时，<code>${{ secrets.GH_PAT }}</code> 会以密文形式注入。这个值只在服务端存在，不会出现在日志里。</p>
<h2 id="github-pages-配置">GitHub Pages 配置</h2>
<p>发布仓库创建好之后，进 <code>mozaitii.github.io</code> → <strong>Settings → Pages</strong>：</p>
<ul>
<li><strong>Build and deployment → Source</strong> 选 <strong>Deploy from a branch</strong></li>
<li><strong>Branch</strong> 选 <code>main</code>，目录选 <code>/ (root)</code></li>
</ul>
<p>因为仓库名是 <code>username.github.io</code> 这种<code>用户站点</code>格式，Pages 默认就给它分配 <code>https://mozaitii.github.io/</code> 域名，无需手动绑定。<code>config.yaml</code> 里的 <code>baseurl</code> 也写死成这个域名，保证站点内的绝对链接指向正确。</p>
<p>一个容易踩的坑：<code>用户站点要求仓库名和 GitHub 用户名完全一致</code>。比如 <code>mozaitii</code> 这个账号，仓库必须叫 <code>mozaitii.github.io</code>，否则 Pages 不会以默认域名发布。</p>
<h2 id="本地到线上">本地到线上</h2>
<p>日常写博客，只需要走本地流程：</p>
<pre><code class="language-bash">hugo server -D   # 本地预览（含草稿）
# ……改完内容后
hugo             # 可选：本地看一眼最终产物（输出到 docs/）
git add .
git commit
git push origin main    # ← 到这里就结束了
</code></pre>
<p>push 之后，GitHub Actions 在云端重新构建并发布，几分钟内 <code>https://mozaitii.github.io/</code> 就是最新版。</p>
<p>另外还给仓库配了一个 pre-commit 钩子（<code>.githooks/</code>），提交前自动把待提交的大图压到 1600px。这条流水线和图片压缩是两条独立的链路：<code>压缩管的是「源码怎么进仓库」，部署管的是「源码怎么变成线上站」</code>，互不干扰。</p>
<h2 id="小结">小结</h2>
<p>本地写博客，push后自动部署，又有了重新更新博的动力 🚣‍♀️。</p>

        
        ]]></description></item></channel></rss>