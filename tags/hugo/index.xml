<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>Hugo on Mozaitii</title><link>https://mozaitii.github.io/tags/hugo/</link><description>Recent content in Hugo on Mozaitii</description><generator>Hugo -- gohugo.io</generator><language>en-US</language><managingEditor>Mozaitii@qq.com (Mozaitii)</managingEditor><webMaster>Mozaitii@qq.com (Mozaitii)</webMaster><lastBuildDate>Sun, 02 Aug 2026 00:00:00 +0000</lastBuildDate><atom:link href="https://mozaitii.github.io/tags/hugo/" rel="self" type="application/rss+xml"/><item><title>博客图片压缩</title><link>https://mozaitii.github.io/cn/2026/08/02/image-compression/</link><pubDate>Sun, 02 Aug 2026 00:00:00 +0000</pubDate><author>Mozaitii</author><guid>https://mozaitii.github.io/cn/2026/08/02/image-compression/</guid><description><![CDATA[
        <p>写动态配图时放了 10 张手机原图，加起来近 24MB。虽然图片设了懒加载，但一滚动就会全量加载，页面明显变慢。这篇记录一下给博客做的图片压缩方案。</p>
<h2 id="先澄清一个误区">先澄清一个误区</h2>
<p>一开始我以为 Hugo 编译会自动压缩图片，查了之后发现不会：<code>static/</code> 目录下的文件是<strong>原样拷贝</strong>到发布目录的，一个字节都不动。Hugo 确实有图片处理能力（<code>.Resize</code>、<code>.Fit</code> 以及 <code>imaging</code> 配置），但只对走资源管线的图片生效——需要图片是页面 bundle 资源，并且模板里显式调用处理函数。我的动态图是 <code>static/</code> + frontmatter 里纯 URL 的方式，完全绕开了这条管线，所以压缩得自己做。</p>
<h2 id="方案-上传前批量压缩">方案：上传前批量压缩</h2>
<p>写了一个脚本 <code>tools/compress_images.py</code>，基于 Python 的 Pillow 库：</p>
<ul>
<li>最长边缩到 1600px，保持比例</li>
<li>转成渐进式 JPEG，质量 82，顺带去掉 EXIF（更小也更利于隐私）</li>
<li>压缩后如果反而变大，保留原图不覆盖</li>
<li>原图放 <code>raw/</code> 目录（已加入 <code>.gitignore</code>），不入库</li>
</ul>
<p>日常用法：</p>
<pre><code class="language-bash"># 新照片：把原图丢进 raw/，一条命令压缩进动态图目录
python3 tools/compress_images.py

# 日志配图：换输出目录即可
python3 tools/compress_images.py --out-dir static/images/posts

# 想更小可以转 WebP（记得同步改 frontmatter 后缀）
python3 tools/compress_images.py --format webp
</code></pre>
<h2 id="实际效果">实际效果</h2>
<p>把已有的 10 张图压了一遍：</p>
<ul>
<li>压缩前 <strong>24.0MB</strong>，压缩后 <strong>1.9MB</strong>，省了 <strong>92%</strong></li>
<li>单张最大的 4.6MB → 443KB，最长边统一 1600px</li>
<li>原图备份在 <code>raw/_originals/</code>，不入库</li>
</ul>
<p>网页上 1600px 宽、一两百 KB 的图已经很清晰，手机上看绰绰有余，加载速度快了一个数量级。</p>
<h2 id="一点感受">一点感受</h2>
<p>记录这件事，也是提醒自己：凡是发到网上的照片，都要先过一道压缩。工具不复杂，难的是坚持用起来。省下来的流量和加载时间，会让博客一直保持轻盈。</p>

        
        ]]></description></item></channel></rss>